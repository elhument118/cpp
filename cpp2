# 📘 C++ 객체지향 및 핵심 개념 상세 정리 (최종 통합본)

---

# 1\. 객체지향(OOP)의 4대 요소

### ✔ 추상화 (Abstraction)

-   복잡한 시스템을 단순화하여 핵심 기능만 보여주는 것
-   **예:** 자동차 클래스 → 엔진 내부 구현 X, drive() 같은 기능만 제공

### ✔ 캡슐화 (Encapsulation)

-   데이터 + 메서드를 하나로 묶고
-   외부 직접 접근을 막고, 메서드를 통해서만 조작하도록 설계
-   **예:** private int balance; → deposit()/withdraw()로 접근

### ✔ 상속 (Inheritance)

-   기존 클래스의 속성과 기능을 새로운 클래스가 물려받음
-   **예:** class Student : public Person {}

### ✔ 다형성 (Polymorphism)

-   같은 인터페이스로 다른 동작 수행
-   **예:** draw() → Circle은 원, Rectangle은 사각형

---

# 2\. 접근 권한 지정자 (Access Specifiers)

지정자 접근 가능 범위

| public | 어디서나 접근 가능 |
| --- | --- |
| protected | 클래스 내부 + 자식 클래스 |
| private | 클래스 내부에서만 가능 |

👉 **Tip:** 멤버 변수는 보통 private, 접근은 getter/setter로 관리.

---

# 3\. 클래스와 객체

```
class Person {
private:
    int age;
public:
    void setAge(int a) { age = a; }
    int getAge() { return age; }
};

Person p;
p.setAge(20);
cout << p.getAge();
```

-   **클래스 = 설계도**, **객체 = 실체(Instance)**

---

# 4\. 생성자 & 소멸자

### ✔ 생성자(Constructor)

-   클래스 이름과 동일
-   반환형 없음
-   객체 생성 시 자동 호출
-   오버로딩 가능

```
class A {
public:
    A() { cout << "기본 생성자"; }
    A(int x) { cout << "매개변수 생성자"; }
};
```

### ✔ 소멸자(Destructor)

-   ~클래스이름
-   객체 소멸 시 자동 호출
-   **오버로딩 불가**

```
~A() { cout << "소멸자"; }
```

---

# 5\. this 포인터

-   객체 자기 자신을 가리킴
-   멤버 변수와 매개변수 이름 충돌 시 사용

```
void setValue(int value) {
    this->value = value;
}
```

---

# 6\. const 멤버 & const 객체

### ✔ const 멤버 함수

-   멤버 대신 변경 불가

```
int get() const { return data; }
```

### ✔ const 객체

```
const A obj;
obj.get();   // 가능
obj.set();   // 불가능
```

---

# 7\. 동적 메모리 할당 (new / delete)

```
int* p = new int(10);
delete p;

int* arr = new int[5];
delete[] arr;
```

👉 delete를 안 하면 **메모리 누수** 발생.

---

# 8\. 함수 오버로딩 (Function Overloading)

-   함수 이름 동일
-   매개변수 타입/개수 다름
-   컴파일 시 결정 **(정적 바인딩)**

```
void print(int x);
void print(double y);
```

---

# 9\. 함수 중첩 (Function Nesting)

한 멤버 함수가 다른 멤버 함수를 호출하는 것.

```
void A() { B(); }
```

---

# 10\. 디폴트 매개변수 (Default Arguments)

```
int sum(int a, int b = 10);
sum(5); // sum(5,10)
```

---

# 11\. 상속(Inheritance)

```
class B : public A {};
```

### ✔ 상속 접근 제어

부모 멤버 public 상속 protected 상속 private 상속

| public | public | protected | private |
| --- | --- | --- | --- |
| protected | protected | protected | private |

### protected 멤버

-   자식 클래스에서 접근 가능
-   외부에서는 접근 불가

---

# 12\. 상속에서 생성자와 소멸자

### ✔ 실행 순서

1.  **부모 생성자**
2.  **자식 생성자**
3.  **자식 소멸자**
4.  **부모 소멸자**

```
class B : public A {
public:
    B() : A() {} // 부모 생성자 호출
};
```

---

# 13\. 다중 상속 (Multiple Inheritance)

```
class C : public A, public B {};
```

👉 다이아몬드 상속 문제 발생 가능 → virtual 상속으로 해결.

---

# 14\. 바인딩 (Binding)

### ✔ 정적 바인딩 (Static / Early)

-   컴파일 시 결정
-   오버로딩, 일반 멤버 함수

### ✔ 동적 바인딩 (Dynamic / Late)

-   실행 시간에 결정
-   virtual 함수 + 오버라이딩

---

# 15\. 오버라이딩 (Overriding)

-   상속 관계
-   부모 함수와 **이름/매개변수/반환형 동일**
-   virtual 필요

```
class A { virtual void show(); };
class B : public A { void show() override; };
```

---

# 16\. static 멤버

### ✔ static 변수

-   클래스 전체가 공유하는 변수
-   메모리는 한 번만 생성됨

```
static int count;
int Class::count = 0;
```

### ✔ static 함수

-   객체 없이 호출 가능

---

# 17\. 가상 함수 & 순수 가상 함수

### ✔ 가상 함수

```
virtual void draw();
```

### ✔ 순수 가상 함수 (추상 클래스)

```
virtual void draw() = 0;
```

---

# 18\. 템플릿 (Template)

### ✔ 템플릿 함수

```
template<typename T>
T add(T a, T b) { return a + b; }
```

### ✔ 템플릿 클래스

```
template<class T>
class Box { T data; };
```

---

# 19\. STL (Standard Template Library)

### ✔ 컨테이너(Container)

-   vector
-   list
-   deque
-   stack
-   queue
-   map
-   set

### ✔ 반복자(Iterator)

컨테이너 순회용 포인터 같은 객체.

### ✔ 알고리즘

```
sort(v.begin(), v.end());
find(v.begin(), v.end(), value);
```

---

# 20\. 콘솔 입출력

객체 설명

| cin | 입력 |
| --- | --- |
| cout | 출력 |
| cerr | 즉시 오류 메시지 |
| clog | 버퍼링되는 로그 메시지 |

---

# 21\. ios 클래스

스트림의 상태 확인용 함수 제공.

함수 의미

| good() | 정상 |
| --- | --- |
| fail() | 실패 |
| eof() | 파일 끝 |
| bad() | 심각한 오류 |

---

# 22\. 입출력 조절자 (Manipulators)

조절자 기능

| setw(n) | 출력 폭 |
| --- | --- |
| setfill(c) | 채우기 문자 |
| setprecision(n) | 소수점 자리수 |
| fixed | 고정 소수점 출력 |
| hex / dec / oct | 진법 변환 |

---

# 23\. 파일 입출력 (File I/O)

### ✔ 기본 스트림

스트림 기능

| ifstream | 파일 입력 |
| --- | --- |
| ofstream | 파일 출력 |
| fstream | 입출력 모두 가능 |

### ✔ 예제

```
ifstream fin("a.txt");
ofstream fout("b.txt");

string s;
getline(fin, s);
fout << s;

fin.close();
fout.close();
```

---

# 🎯 전체 범위 30초 초압축 요약

-   OOP = 추상화·캡슐화·상속·다형성
-   접근권한 = public/protected/private
-   생성자/소멸자 = 자동 실행
-   this = 객체 자신
-   const = 변경 불가
-   new/delete = 동적 메모리
-   오버로딩 = 정적 바인딩
-   오버라이딩 = virtual + 동적 바인딩
-   static = 클래스 공통
-   템플릿/STL = 범용 자료구조/알고리즘
-   파일 입출력 = 스트림 기반

---
